#include<stdio.h>
#include<conio.h>
int main()
{
  int a,b;
  printf("Enter the number a:");
  scanf("%d", &a);
  printf("Enter the number b:");
  scanf("%d", &b);
  
  printf("The sum of two nmbers is %d", a+b);
  return 0;
}
